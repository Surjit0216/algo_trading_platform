<!-- # Algo Trading Platform

This repository contains a React dashboard that reads trading logs from a Google Sheet and displays performance metrics.

See `dashboard/README.md` for usage instructions.


When deploying to Vercel, add an environment variable named
`VITE_SHEET_CSV_URL` that points to the CSV export of your Google Sheet.
Without this variable the dashboard will render but not display any data.

 -->